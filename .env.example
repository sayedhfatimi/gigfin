DB_FILE_NAME=file:./data/db.sqlite
# Generate a new secret with `npx @better-auth/cli@latest secret` or `openssl rand -base64 32`
BETTER_AUTH_SECRET=your-secret
# Public URL where your GigFin instance is accessible (e.g., https://gigfin.me)
BETTER_AUTH_URL=http://localhost:3000
# Optional override for the internal API host (defaults to localhost during development)
INTERNAL_API_BASE=http://localhost:3000
# Optional: external analytics script (e.g. Rybbit, Plausible, Umami)
ANALYTICS_SCRIPT_URL=
ANALYTICS_SITE_ID=
