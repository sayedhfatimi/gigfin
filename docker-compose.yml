version: "3.9"

services:
  gigfin:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - gigfin-data:/app/data
    restart: unless-stopped

volumes:
  gigfin-data:
